{
  "name": "gateway-service",
  "version": "1.0.0",
  "description": "getaway service",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
